@{
    ViewData["Title"] = "Home Page";
}

<a href="Employee" class="btn btn-default">EmployeeIndex</a>

<a id="addRandom" class="btn btn-danger">Add Random Product</a>

<a id="addRandomCategory" class="btn btn-danger">Add Random Category</a>
<a id="seedProducts" class="btn btn-danger">Seed products</a>

@*<button id="h" type="button" class="btn btn-sm btn-default">Push Data</button>

<p id="msg"></p>*@

@*@section scripts{
    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script src="~/js/SignalR/jquery.signalR-2.2.0.js"></script>
    <script src="/signalr/hubs"></script>

    <script>
        let test = $.connection.test;
        let hub = $.connection.hub;

        hub.start().done(function () {
            test.server.signalRConnectionId("User", 0)
        });

        var id;

        test.client.getConnectionId = function (msg) {
            id = msg;
            console.log(id);
        };

        $("#h").click(function () {
            hub.start().done(function () {
                test.server.addObject(id, { User: "User", BusinessId: 1, OrderId: 1 });
            });
        });

        test.client.getMessage = function (msg) {
            var message = msg;
            console.log("Message: " + message);
            $("#msg").text(message);
        }

        hub.start();
    </script>
}*@

